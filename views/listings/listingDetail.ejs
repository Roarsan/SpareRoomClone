<%- include('../partials/navbar') %>

<div class="card mx-auto shadow-sm" style="max-width: 600px;">
  <img src="<%= list.image %>" class="card-img-top" alt="<%= list.title %>">
  <div class="card-body">
    <h4 class="card-title fw-bold"><%= list.title %></h4>
    <p class="text-muted mb-2"><%= list.address %></p>
    <h5 class="text-primary fw-bold">£<%= list.price %> / month</h5>
    <p class="mt-3"><%= list.description %></p>
  </div>
</div>

<div class="d-flex justify-content-center gap-3 mt-4">
  <a href="/list/<%= list._id %>/editlisting" class="btn btn-outline-primary">Update</a>

 <!-- Delete Button (Triggers Modal) -->
<button type="button" class="btn btn-outline-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
  Delete
</button>
</div>

<%- include('deletelisting', { list: list }) %>
<%- include('../partials/footer') %>
